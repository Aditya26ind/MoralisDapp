import React from 'react';
import { Button } from 'antd';

const Checkout = () => (
  <div className="container">
    <Button type="primary">Checkout Page</Button>
  </div>
);

export default Checkout;